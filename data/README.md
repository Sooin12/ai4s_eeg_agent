# 数据目录

数据遵循 `raw -> interim -> processed` 的单向流转规则。

- `raw/`：官方原始下载，只读保存。
- `interim/`：解压、格式转换和质量检查产生的中间文件。
- `processed/`：已经通过版本化、数据集无关语义合同和确定性验收、可供后续阶段消费的数据。
- `manifests/`：文件路径、大小、哈希、数据版本和验收报告。

不要复制完整数据来创建实验版本。实验差异应由配置描述，运行结果写入 `artifacts/runs/`。
