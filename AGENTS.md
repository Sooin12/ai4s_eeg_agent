# 项目内 Agent 开发约束

## 不可偏离的目标

本项目开发的是一个可审计的科研 Agent 系统，而不是由开发者或外部大模型手工完成 EEG 分析。

系统必须保持数据集无关。任何具体数据集都只能作为仓库外测试输入或由通用格式适配器处理的验收夹具；Agent编排、阶段合同、搜索、验证和报告逻辑不得依赖某个数据集ID。新数据必须先经过结构探测、适配器选择和标准画像验收，后续Agent只能消费标准合同，不得直接读取某个数据集的私有目录约定。

系统必须能够自主完成以下闭环：

1. 数据集级理解：实验范式、动作类型、静息态、通道、设备、事件、会话和数据量；
2. 根据数据契约约束合法的全流程搜索空间；
3. 生成被试级画像：噪声、坏道、个体频率、ERD/ERS、稳定性和跨会话漂移；
4. 在固定预算内搜索完整分析路径：预处理、分段、特征、模型和验证；
5. 使用未参与搜索的独立会话确认，并输出证据化、可复现的解释。

开发者手工发现的结论只能作为测试基准。任何正式能力都必须落实为 Agent 可调用的工具、规则、契约或自动化测试。

## 技术边界

- 面向用户的Agent说明、进度和结论默认使用简体中文；机器合同字段、ID和文献标识保持原文。
- 原始 EEG、数值计算、训练、统计检验和指标判断由本地确定性程序执行。
- LLM 负责语义理解、规划、工具选择、失败诊断和证据组织，不直接读取大规模原始 EEG 数组。
- 模型供应商必须可替换；核心逻辑不得绑定 DeepSeek、Kimi 或 OpenAI。
- API 密钥只从环境变量读取，不得写入源码、配置、日志或审计产物。
- 每次运行必须记录工具参数、结果、错误、预算和最终状态，支持完整重放。

## 自主权边界

本系统评价的是科研 Agent 能否在固定资源和数据边界内自主完成研究闭环，而不是用户逐项替 Agent 做科研决策。每次正式研究运行开始前只建立一次 `AutonomyEnvelope`，至少声明：研究目标、可访问数据、禁止动作、计算与 API 总预算、最大科研周期数以及冻结确认访问策略。

在 `AutonomyEnvelope` 内，Agent 必须自主完成并留下证据，不得要求用户逐项批准：

- 搜索数据、画像数据与冻结确认数据的划分；
- 评价指标、统计检验、成功/拒绝门槛和有限个体 oracle 的定义；
- 合法搜索空间、候选组件、质量控制和逻辑排除政策；
- 实验顺序、失败恢复、预算分配、停止条件与 pipeline 选择；
- pipeline 锁定后的首次 frozen confirmation；
- 内部科研结论、负结果、不确定性与证据报告。

上述决策必须在观察相应结果前由专门 Agent 提出，经独立 Critic Agent 和本地确定性验证器检查后自动冻结。Critic 不得与 Planner 共用未公开的确认结果。冻结确认数据只能由确定性访问控制器在 pipeline hash、协议 hash、预算和阶段账本均满足条件时一次性解锁；确认结果不得触发反向调参。证据不足时 Agent 应拒绝强结论，而不是把决定转交给用户。

只有以下超出既定授权或具有不可逆/外部责任的事项必须暂停并请求用户审计：

- 改变研究目标，或扩大 `AutonomyEnvelope` 的数据、计算、API 付费或科研周期预算；
- 删除、覆盖或不可逆修改原始数据与历史审计产物；
- 许可证、伦理、隐私、数据权属或对外披露边界无法由现有证据确定；
- 重复访问已经消费过的 frozen confirmation 数据，或在确认后重开同一研究的搜索；
- 将内部证据报告升级为论文、比赛材料、公开宣传或其他对外成果声明；
- 需要新的账号权限、外部协调或法律责任主体决策。

逻辑排除不是原始数据删除。Agent 可以依据预冻结规则把被试、run、trial 或通道标记为不进入某次计算，但必须保留原始数据、逐项理由、计数、规则版本和可重放记录。

只读检查、可逆工程实现、离线测试以及 `AutonomyEnvelope` 内的自主科研执行不应暂停等待人工审批。
